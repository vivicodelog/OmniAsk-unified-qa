# ============================================================
# Unified QA — 文档+数据统一问答平台
# 纯国内环境，无 GPU，无梯子
# ============================================================

# --- Web Framework ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# --- Data Processing ---
openpyxl>=3.1          # Excel .xlsx 读写
pandas>=2.0            # CSV 解析 + DataFrame 操作
xlrd>=2.0              # 旧版 .xls 文件（备选）
jieba>=0.42            # 中文分词（关键词检索用）

# --- PDF Processing ---
PyMuPDF>=1.23          # PDF 文本/图片提取
pdfplumber>=0.10       # PDF 表格检测

# --- AI / LLM ---
langchain>=0.1
chromadb==1.5.9        # 向量库（本地持久化，固定版本防漂移）
openai>=1.0            # DeepSeek API 兼容 OpenAI 格式
sentence-transformers>=2.5  # BGE-M3 本地 CPU Embedding
modelscope>=1.10       # 魔搭社区模型下载

# --- Validation ---
python-dotenv>=1.0
pydantic>=2.5
pymysql>=1.1

# --- Async HTTP ---
httpx>=0.25
aiofiles>=23.0

# --- Testing ---
pytest>=7.4
pytest-asyncio>=0.23
